<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.ourhouse.mapper.DashBoardMapper">

 
<select id="getAverages" parameterType="long" resultType="map">
	SELECT year, avg(price) average, name from static where dong_code = #{dongCode} group by year;
</select>

<select id="getYearAmount" parameterType="long" resultType="map">
	SELECT year, sum(count) count from static where dong_code = #{dongCode} group by year;
</select>

<select id="getMonthAmount" parameterType="long" resultType="map">
	SELECT month, sum(count) count from static where dong_code =  #{dongCode} group by month;
</select>

<select id="getPolygon" parameterType="long" resultType="map">
	SELECT name, geo, c_lat, c_lng from polygon where dong_code = #{dongCode}
</select>

 <select id="getGender" parameterType="long" resultType="map">
	select count(*) count, gender from user where favorite_area = #{dongCode} group by gender;
</select>	

<select id="getAge" parameterType="long" resultType="map">
	select count(*) count, age_group from user where favorite_area = #{dongCode} group by age_group;
</select> 

</mapper>

